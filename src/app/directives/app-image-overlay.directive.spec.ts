import { AppImageOverlayDirective } from './app-image-overlay.directive';

describe('AppImageOverlayDirective', () => {
  it('should create an instance', () => {
    const directive = new AppImageOverlayDirective();
    expect(directive).toBeTruthy();
  });
});
